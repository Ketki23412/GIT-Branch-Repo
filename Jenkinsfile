pipeline (

  agent any(
        stages(
          stage("pipeline")
            steps(
              echo "this is multibranch"
               )
          )
    )
  )
  


pipeline (

  agent any(
        stages(
          stage("pipeline")
            steps(
              echo "this is v1 multibranch"
               )
          )
    )
  )
  
